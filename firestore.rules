# Firebase Security Rules for HBS2026 Live-Scoring

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Clubs collection - read access for all users, write access for authenticated users
    match /clubs/{clubId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
